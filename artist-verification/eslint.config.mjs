/**
 * Repo-root `eslint .` discovers this file. Do not import this package's
 * ESLint plugins here — they are not installed at the repo root.
 * Local lint: `npm run lint` → eslint.app.config.mjs.
 */
export default [{ ignores: ["**/*"] }];
